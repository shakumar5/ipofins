# dependencies
node_modules/

# build output
dist/

# environment variables
.env
.env.*
!.env.example

# editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Astro
.astro/

# Market data — stored in Neon DB, never committed
src/data/ipos.json
src/data/mutual-funds.json
src/data/fund-holdings.json
src/data/upcoming-ipos.json
src/data/ipo-performance.json
src/data/ipos_pretty.json
data/amfi-excel/
data/temp/

# Deprecated fetch backups
scripts/ipos_*.json

# Local dev logs & script output (not needed for prod build)
build-log.txt
check-output.txt
scripts/output/

# Internal specs & SEO audit notes (not needed for prod build)
.kiro/
SEO_AUDIT_FINAL.md
SEO_STRATEGY.md

# Client JSON — regenerated on every `npm run build` / CI via export-client-data.mjs
public/data/**
!public/data/.gitkeep

# Sitemaps — regenerated during export / build
public/sitemap-*.xml
public/sitemap-overlap-staging-*.xml

# OG PNGs — rasterized from public/og-*.svg during build
public/og-*.png
