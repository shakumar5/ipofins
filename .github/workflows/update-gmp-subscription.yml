# DEPRECATED — replaced by manual pipeline scripts.
# See DATA_PIPELINE.md
#
# Run locally:
#   npm run pipeline:subscription
#
# This workflow is disabled. Delete this file when convenient.

name: Update GMP & Subscription (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use npm run pipeline:subscription locally."
